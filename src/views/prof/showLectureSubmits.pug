extends ../base.pug
block content
    h2  #{lecture.lectureName} answer

    if filenames.length != 0
        each  item in filenames 
            tr    
                td
                    h4=item.student.name  
                td
                    a(href=`/prof/lecture/gradeStudentSubmit/${lecture._id}/${item.student.stuId}`) #{item.filename}
               
    else 
        h3(style="color:red") no submitted assignment
        